'use strict';

const HD_PREFIX = "m/44'/508'/0'/0'";
const HD_PATH = `${HD_PREFIX}/0'`;

const MNEMONIC_LEN = 256;
const ERD = 'erd';

const ACC_TYPE_WEB = 'web';
const ACC_TYPE_LEDGER = 'ledger';

module.exports = {
  HD_PREFIX,
  HD_PATH,
  MNEMONIC_LEN,
  ERD,
  ACC_TYPE_WEB,
  ACC_TYPE_LEDGER,
};
